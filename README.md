# Assignment 3 — Algorithm Efficiency and Scalability

This contains beginner-friendly, runnable code and a short report for:

- Randomized Quicksort vs Deterministic Quicksort (first-element pivot)
- Hash Table with Chaining (insert, search, delete, automatic resizing)


# run quicksort demo
python quicksort.py

# run benchmarks (creates quicksort_results.csv)
python benchmark_quicksort.py

# run hash table tests
python tests_hash_table.py

Files
`quicksort.py` — both quicksort implementations + small demo
`benchmark_quicksort.py` — benchmarks across input sizes & distributions; writes `quicksort_results.csv`
`hash_table_chaining.py` — chaining hash table with load-factor-based resizing
`tests_hash_table.py` — simple functional tests for the hash table
`report.md` — theory, methods, and results summary (edit to add your own comments/screenshots)


